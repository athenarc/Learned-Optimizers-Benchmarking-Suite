{
  "query": "/*+ NoMergeJoin(ci rt t mc) NoHashJoin(ci mc rt t cn) NoHashJoin(ci cn mc rt t n1) NoMergeJoin(ci cn mc n1 rt t an1) */ select min(an1.name) AS costume_designer_pseudo,\nmin(t.title) AS movie_with_costumes\nfrom role_type AS rt\ninner join cast_info AS ci\non ci.role_id = rt.id AND rt.role = 'miscellaneous crew'\ninner join title AS t\non ci.movie_id = t.id\ninner join movie_companies AS mc\non ci.movie_id = mc.movie_id AND t.id = mc.movie_id\ninner join company_name AS cn\non mc.company_id = cn.id AND cn.country_code = '[us]'\ninner join name AS n1\non n1.id = ci.person_id\ninner join aka_name AS an1\non an1.person_id = ci.person_id AND an1.person_id = n1.id;",
  "predicted_latency": 488.34604024887085,
  "actual_latency": 21091.056,
  "q_error": 43.18875195394556,
  "inference_time": 0.04678773880004883
}