{
  "query": "/*+ NoMergeJoin(an n) NoHashJoin(an n ci) NoHashJoin(an ci n t) NoHashJoin(an ci n t chn) NoMergeJoin(an chn ci n t rt) NoMergeJoin(an chn ci n rt t mc) */ select min(an.name) AS alternative_name,\nmin(chn.name) AS voiced_character_name,\nmin(n.name) AS voicing_actress,\nmin(t.title) AS american_movie\nfrom aka_name AS an\ninner join name AS n\non an.person_id = n.id AND n.gender = 'f' AND n.name like '%Jo%'\ninner join cast_info AS ci\non n.id = ci.person_id AND an.person_id = ci.person_id AND ci.note IN ('(writer)',\n'(producer)',\n'(voice)',\n'(executive producer)')\ninner join title AS t\non ci.movie_id = t.id\ninner join char_name AS chn\non chn.id = ci.person_role_id\ninner join role_type AS rt\non ci.role_id = rt.id AND rt.role = 'actress'\ninner join movie_companies AS mc\non t.id = mc.movie_id AND ci.movie_id = mc.movie_id\ninner join company_name AS cn\non mc.company_id = cn.id AND cn.country_code = '[us]';",
  "predicted_latency": 278.97873520851135,
  "actual_latency": 639.613,
  "q_error": 2.2926944575970896,
  "inference_time": 0.04509854316711426
}