{
  "query": "/*+ NoHashJoin(mc cn) NoMergeJoin(cn mc t) NoHashJoin(cn mc t ci) NoHashJoin(ci cn mc t an1) NoHashJoin(an1 ci cn mc t n1) NoHashJoin(an1 ci cn mc n1 t rt) */ select min(an1.name) AS costume_designer_pseudo,\nmin(t.title) AS movie_with_costumes\nfrom movie_companies AS mc\ninner join company_name AS cn\non mc.company_id = cn.id AND cn.country_code = '[jp]'\ninner join title AS t\non t.id = mc.movie_id\ninner join cast_info AS ci\non ci.movie_id = t.id AND ci.movie_id = mc.movie_id\ninner join aka_name AS an1\non an1.person_id = ci.person_id\ninner join name AS n1\non an1.person_id = n1.id AND n1.id = ci.person_id\ninner join role_type AS rt\non ci.role_id = rt.id AND rt.role = 'actress';",
  "predicted_latency": 602.3679971694946,
  "actual_latency": 9738.229,
  "q_error": 16.166577649808065,
  "inference_time": 0.03728771209716797
}